const calculateHoverSquares = (squares, type) => {
  const line = Array(Number(type));
  console.log(line);
  console.log("ssss", squares);
  //   for (let i = 0; i < line.length; i += 1) {
  //     if (squares ) {
  //       return squares;
  //     }
};

export default calculateHoverSquares;
